// Placeholder for the post-B.1 property service module. The legacy implementation
// (`loadGuestProperties`, property override map, `getGuestProperty`,
// `setGuestPropertyOverride`) lives in `jni/vm_native_bridge.cpp`. Phase C.3 will extract
// this when the property service is upgraded to a real `__system_property_*` source.
